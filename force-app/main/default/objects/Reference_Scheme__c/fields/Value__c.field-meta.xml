<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Value__c</fullName>
    <externalId>false</externalId>
    <formula>IF(ISPICKVAL( Activity__c , &apos;Telephone Reference&apos; ), 100, 

IF(ISPICKVAL( Activity__c, &apos;Site Reference&apos; ), 500, 

IF(ISPICKVAL( Activity__c, &apos;Webinar Presentation&apos; ), 500, 

IF(ISPICKVAL( Activity__c, &apos;Case Study&apos; ), 1000, 

IF(ISPICKVAL( Activity__c, &apos;Event Speaker&apos; ), 1000,0)))))</formula>
    <formulaTreatBlanksAs>BlankAsZero</formulaTreatBlanksAs>
    <label>Initial Value</label>
    <precision>18</precision>
    <required>false</required>
    <scale>2</scale>
    <trackHistory>false</trackHistory>
    <trackTrending>false</trackTrending>
    <type>Number</type>
    <unique>false</unique>
</CustomField>
